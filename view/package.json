{
  "name": "ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:css && npm run build:ts",
    "build:ts": "esbuild --minify --outdir=../assets/js ./ts/**/*.ts",
    "build:css": "tailwindcss -i ./css/styles.css -o ../assets/styles.css"
  },
  "author": "piotrek",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.22.0",
    "tailwindcss": "^3.4.4",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
